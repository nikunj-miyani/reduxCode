import { Dimensions } from "react-native";
const gScreenWidth = Dimensions.get("screen").width;
const gScreenHeight = Dimensions.get("screen").height;
const gWindowWidth = Dimensions.get("window").width;
const gWindowHeight = Dimensions.get("window").height;
// iphone 12 wise font-size

// console.log(gScreenWidth * 0.075));
export default {
  // f8: Math.ceil(gScreenWidth * 0.02),
  // f9: Math.ceil(gScreenWidth * 0.022),
  // f10: Math.ceil(gScreenWidth * 0.025),
  // f11: Math.ceil(gScreenWidth * 0.027),
  // f12: Math.ceil(gScreenWidth * 0.03),
  // f13: Math.ceil(gScreenWidth * 0.033),
  // f14: Math.ceil(gScreenWidth * 0.035),
  // f15: Math.ceil(gScreenWidth * 0.037),
  // f16: Math.ceil(gScreenWidth * 0.039),
  // f17: Math.ceil(gScreenWidth * 0.042),
  // f18: Math.ceil(gScreenWidth * 0.045),
  // f19: Math.ceil(gScreenWidth * 0.047),
  // f20: Math.ceil(gScreenWidth * 0.05),
  // f21: Math.ceil(gScreenWidth * 0.052),
  // f22: Math.ceil(gScreenWidth * 0.055),
  // f23: Math.ceil(gScreenWidth * 0.057),
  // f24: Math.ceil(gScreenWidth * 0.059),
  // f25: Math.ceil(gScreenWidth * 0.062),
  // f26: Math.ceil(gScreenWidth * 0.065),
  // f27: Math.ceil(gScreenWidth * 0.067),
  // f28: Math.ceil(gScreenWidth * 0.07),
  // f29: Math.ceil(gScreenWidth * 0.072),
  // f30: Math.ceil(gScreenWidth * 0.075),

  f8: gScreenWidth * 0.02,
  f9: gScreenWidth * 0.022,
  f10: gScreenWidth * 0.025,
  f11: gScreenWidth * 0.027,
  f12: gScreenWidth * 0.03,
  f13: gScreenWidth * 0.033,
  f14: gScreenWidth * 0.035,
  f15: gScreenWidth * 0.037,
  f16: gScreenWidth * 0.039,
  f17: gScreenWidth * 0.042,
  f18: gScreenWidth * 0.045,
  f19: gScreenWidth * 0.047,
  f20: gScreenWidth * 0.05,
  f21: gScreenWidth * 0.052,
  f22: gScreenWidth * 0.055,
  f23: gScreenWidth * 0.057,
  f24: gScreenWidth * 0.059,
  f25: gScreenWidth * 0.062,
  f26: gScreenWidth * 0.065,
  f27: gScreenWidth * 0.067,
  f28: gScreenWidth * 0.07,
  f29: gScreenWidth * 0.072,
  f30: gScreenWidth * 0.075,
};
